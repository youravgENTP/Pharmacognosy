import { ImportWorkspace } from "@/components/import-workspace";
export default function ImportPage() { return <div className="page"><header className="page-header"><div><p className="eyebrow">Word / Excel → JSON</p><h1>Import</h1><p className="subtitle">기존 학습 자료를 검증 가능한 구조로 안전하게 가져옵니다.</p></div></header><ImportWorkspace/></div>; }

